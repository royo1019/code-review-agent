[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = ["venv", "tmp", "tests/fixtures"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "B",    # flake8-bugbear
    "UP",   # pyupgrade
    "S",    # flake8-bandit (security)
    "SIM",  # flake8-simplify
]
ignore = [
    "E501",  # line-too-long (handled by formatter)
    "S101",  # assert (fine in tests)
    "B008",  # function-call-in-default-argument (FastAPI uses Depends() pattern)
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S", "B"]
