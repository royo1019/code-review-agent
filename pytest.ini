[pytest]
testpaths = tests
asyncio_mode = auto
log_cli = true
log_cli_level = INFO
markers =
    integration: tests that hit live external services (Groq, GitHub) — deselected by default
addopts = -m "not integration" -ra
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
